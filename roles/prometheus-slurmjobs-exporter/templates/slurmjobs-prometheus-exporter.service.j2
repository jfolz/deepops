[Unit]
Description=Prometheus Node Exporter
After=docker.service
Requires=docker.service

[Service]
TimeoutStartSec=0
Restart=always
ExecStart=python -m uvicorn --host 0.0.0.0 --port 9500 slurmjobs.prometheus_exporter:app

[Install]
WantedBy=multi-user.target
